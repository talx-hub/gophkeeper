coverage:
  round: down
  range: '80...100'
  status:
    patch:
      default:
        target: 80%
    project:
      default:
        target: auto
        threshold: 0%
  ignore:
    - "pkg/session/mocks/*"
    - "internal/api/v1/mocks/*"
    - "proto/*"
